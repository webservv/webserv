http {
    # General settings
    client_max_body_size 100; # Limit client body size

    # Server block 1
    server {
        listen 8080; # Port 80
        # server_name www.helloworld.com; # Hostname
        server_name www.helloworld.com; # Hostname

        error_page 404 error.html; # Default error pages

        root /document/cgi;
        index index.py;

        location / {
            limit_except GET POST DELETE;
            # root /document/cgi;
            index index.py;
        }
        location /test {
            root /document;
            index index.html;
        }

        location /cgi {
            root /document;
            index index.py post.php delete.pl;
            autoindex on; # Directory listing
        }
    }

    # Server block 2
    server {
        listen 4242; # Port 8080
        server_name www.example.com; # Hostname Local host와 비교! http host header field와 비교 -> 일치하는 경우 처리, 없으면 default server block으로 처리

        root /doc2;
        # Redirect to a different URL
        location /redirect {
            return 301 http://www.newsite.com$request_uri;
        }

        # Other settings similar to Server block 1
        # ...
    }

    # Additional server blocks
    # ...
}